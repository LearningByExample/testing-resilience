offers-datasource:
    protocol: "postgresql"
    host: "locahost"
    port: "5432"
    database: "offers"
    credentials:
        user: "sa"
        password: ""
    read-only: false
    pool:
        connections:
            min: 1
            max: 3
    ssl:
        enabled: true
        mode: "allow"
logging:
    level:
        ROOT: DEBUG
spring:
    main:
        banner-mode: console
    jmx:
        enabled: true
management:
    health:
        r2dbc:
            enabled: false
